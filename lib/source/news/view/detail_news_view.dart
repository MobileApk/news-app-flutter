import 'package:flutter/material.dart';

class DetailNewsView extends StatelessWidget {
  const DetailNewsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Detail News View"),
      ),
    );
  }
}